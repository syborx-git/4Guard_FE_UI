# .npmrc
# Necesario para resolver el conflicto de peer dependencies entre:
#   ng2-charts@5.0.4 → @angular/cdk@22.x → @angular/common@^22
#   vs el proyecto que usa Angular 17.
# Sin este flag, npm 11+ (usado por Netlify/CI) falla con ERESOLVE.
legacy-peer-deps=true
